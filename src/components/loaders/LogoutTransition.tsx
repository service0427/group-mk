import React from 'react';

/**
 * 로그아웃 시 화면 전환을 처리하는 컴포넌트 (모든 효과 제거 버전)
 * 로그아웃 시 깨끗하게 바로 로그인 페이지로 이동
 */
export const LogoutTransition: React.FC = () => {
  // 전환 효과 완전히 제거 - 아무 작업도 수행하지 않음
  return null;
};

export default LogoutTransition;