/**
 * 백업 데이터 함수 인터페이스
 * 이 파일은 CampaignTemplate에서 참조하는 파일이 없어서 발생하는 에러를 방지하기 위한 임시 파일입니다.
 */

import { ICampaign } from '../components/CampaignContent';

/**
 * 서비스별 캠페인 데이터를 가져오는 함수 (백업용)
 * @param serviceType 서비스 유형
 * @returns 해당 서비스 유형의 캠페인 데이터
 */
export const getCampaignsByService = (serviceType: string): ICampaign[] => {
  return [];
};