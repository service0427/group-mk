import React from 'react';
import { NoticePageComponent } from './components/NoticePageComponent';

/**
 * 공지사항 관리 페이지 래퍼 컴포넌트
 * 이 컴포넌트는 NoticePageComponent를 감싸는 컨테이너 역할을 합니다.
 */
const NoticePage = () => {
  return <NoticePageComponent />;
};

export default NoticePage;